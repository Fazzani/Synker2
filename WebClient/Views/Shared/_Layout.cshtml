<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Synker</title>
    <base href="~/" />
    <link rel="manifest" href="~/manifest.json">
    <data data-api-url="@ViewBag.ApiUrl" data-ws-url="@ViewBag.WsUrl" />
    <link href="~/dist/site.css" rel="stylesheet" asp-append-version="true" />
    <link rel="icon" href="~/favicon.ico" asp-append-version="true" />
</head>
<body class="noselect">
    @RenderBody()

    @RenderSection("scripts", required: false)
</body>
</html>
